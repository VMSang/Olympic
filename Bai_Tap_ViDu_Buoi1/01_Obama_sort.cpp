#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int n; cin >> n;
	int half = n / 2;
	vector<float> v(half);
	for (int i = 0; i < half; i++){
		cin>>v[i];
	}

	sort(v.begin(), v.end());

	int half2 = n/2 + 1;
	vector<float> v2;
	for(int)
	return 0;
}